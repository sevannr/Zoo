export class Animal{

    constructor(
        public _id: string,
        public name: string,
        public desciption: string,
        public year: number,
        public image: string,
        public userId: string
    ) {
    }
}